# Deprecated legacy example

This file is retained only for historical comparison.

For the local MVP, use:

- `examples/helm-service-values.yaml`
- `examples/helm-ingress-template.yaml`

Those examples target `charts/<service>/values.yaml` and `charts/<service>/templates/ingress.yaml`.
